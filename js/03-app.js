// 3. El Objeto Math
let rMath;

rMath = Math.round(2.8);
rMath = Math.round(2.2);
// 2.5 lo redondea a 3
rMath = Math.round(2.5);

// Redondear hacia arriba = 3
rMath = Math.ceil(2.1);

// Redondear hacia abajo = 2
rMath = Math.ceil(2.9);
// Raiz cuadrada = 2
rMath = Math.sqrt(4);
// Absoluto = 500
rMath = Math.abs(-500);
// Potencia
rMath = Math.pow(2, 2);
// Minimo el menor
rMath = Math.min(3, 1, 12, -3);
// Maximo el mayor
rMath = Math.max(3, 1, 12, -3);
// Aleatorio
rMath = Math.random();
// Aleatorio dentro de un rango (30)
rMath = Math.floor( Math.random() * 30)
console.log(rMath);